https://codepen.io/vannguyen1412/pen/xxdVwoB


<!-- Questions to Submit in today's file first-lastname.txt -->
<!--
Copy the questions below into the first-lastname.txt file on your github repo - answer each question and then push your changes.

    1. What is Semantic HTML? the use of a markup language to reinforce the meanings of each element on the page rather than just define its appearance
    2. What is HTML used for? HTML is used to structure a webpage and its content
    3. What is an attribute and where do we put it? an attribute is a specification that defines a property of an object, element or a file. It is placed in the open tag and usually consists of name/value pair like name="value"
    4. What is the h1 tag used for? How many times should I use it on a page? 
    h1 tag is used to define the header. It is usually used only once in a webpage, and it is normally the most important title of the page
    5. Name two tags that have required attributes
    <img> and <a>
    6. What do we put in the head of our HTML document?
    <!DOCTYPE html>
    7. What is an id?
    an id is an attribute to specify a unique id for html element
    8. What elements can I add an id to?
    we can add an id to any element but can only use once in the whole coding page
    9. How many times can I use the same id on a page?
    1 time only
    10. What is a class?
    a class is an attribute which specifies one or more class names for an element.
    11. What elements can I add a class to?
    any.
    12. How many times can I use the same class on a page?
    multiple times
    13. How do I get my link to open in a new tab?
    <a href="link" target="_blank"></a>
    14. What is the alt attribute in the image tag used for?
    it will show the content in alt attribute in case the image cannot be shown.
    15. How do I reference an id?
	# and the name of the id
    16. What is the difference between a section and a div
    section means that the content inside is grouped and should appear as an entry in an outline of a page. div does not convey any meaning
    17. What is CSS used for?
    to defines style of the web page, including design, layout, variations in display for diff. devices and screen sizes
    18. How to we select an element? Example - every h2 on the page
    simply write h2
    19. What is the difference between a class and an id? - Give me an example of when I might use each one
an id can only be used once while a class can be used multiple times. In case we think that the element is unique and there is no need to group it with other elements in designing, etc. we can use id, or else use class in order to modify them quicker and less mistake because of repeating.
    20. How do we select classes in CSS?
    add . in front of class name.
    21. How do we select a p element with a single class of “human””?
    .human {}
    22. What is a parent child selector? When would this be useful?
    a selector that select all elements that are a direct child of a specified element.
    23. How do you select all links within a div with the class of sidebar?
    name the class and add . in front of the class name when selecting
    24. What is a pseudo selector?
    it is a selector that specifies a special state of the selected elements.
    25. What do we use the change the spacing between lines?
    line-height:
    26. What do we use to change the spacing between letters?
    letter-spacing
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
    text-transform: uppercase;
    28. How do I add a 1px border around my div that is dotted and black?
    border: dotted black 1px;
    29. How do I select everything on the page?
    *{}
    30. How do I write a comment in CSS?
    /*    */
    31. How do I find out what file I am in, when I am using the command line?
    pwd
    32. Using the command line - how do I see a list of files/folders in my current folder?
    ls
    33. How do I remove a file via the command line? Why do I have to be careful with this?
    rm file name. be careful because we cannot undo it.
    34. Why should I use version control?
    there will be many people working together in the same project. There will be many revision in the process of completing it, so it will be better to have a control system in order not to lose track of the newest and to keep history of the old version.
    35. How often should I commit to github?
commit as often as possible. whenever we make changes
    36. What is the command we would use to push our repo up to github?
    git push -u origin branch-name
    37. Walk me through Lambda's git flow.
    fork repo on the remote server
    clone it to the local computer when we want to make any changes
    create a new branch
    make changes
    commit the changes
    push it to Githun
    request pull

Stretch Questions

    1. What is the difference between an inline element and a block element?
    2. What happens when an element is positioned absolutely?
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width?
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them?



   --
